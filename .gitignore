# The directory Mix will write compiled artifacts to.
/code/backend/apps/_build/
/code/frontend/.shadow-cljs/
/_build/
/code/frontend/ap/_build/
# If you run "mix test --cover", coverage assets end up here.
/cover/
/code/frontend/node_modules/
# The directory Mix downloads your dependencies sources to.
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Ignore package tarball (built via "mix hex.build").
phoenix_cljs-*.tar

# If NPM crashes, it generates a log, let's ignore it too.
npm-debug.log

# The directory NPM downloads your dependencies sources to.
/code/backend/apps/postgres/node_modules/
/code/backend/apps/phoenix_cljs/node_modules/
/code/backend/apps/e_meter/node_modules/
/code/backend/apps/postgres/deps/
/code/backend/apps/phoenix_cljs/deps 
/code/backend/apps/e_meter/deps 
/code/frontend/assets/node_modules/

# Since we are building assets from assets/,
# we ignore priv/static. You may want to comment
# this depending on your deployment strategy.
/priv/static/
